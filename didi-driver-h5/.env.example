# 司机 H5 联调：API 根地址（不要尾斜杠）。示例页会请求 ${VITE_API_BASE_URL}/actuator/health。
#
# 经 Spring Cloud Gateway（默认 18080）访问。
VITE_API_BASE_URL=http://127.0.0.1:18080
#
# 直连 driver-api（默认 8101）时改为：http://127.0.0.1:8101（直连时同样注意 BFF 侧 CORS，与后端配置一致）
#
# WebSocket 直连地址（可选）：若网关未配置 WS 转发，建议设置为 driver-api 直连。
# 例：VITE_DRIVER_WS_BASE_URL=ws://127.0.0.1:8101
