# 乘客 H5 联调：API 根地址（不要尾斜杠）。示例下单 POST ${VITE_API_BASE_URL}/app/api/v1/orders。
#
# 经 Spring Cloud Gateway（默认 8080）访问。
VITE_API_BASE_URL=http://127.0.0.1:8080
#
# 直连 passenger-api（默认 8100）时改为：http://127.0.0.1:8100（路径仍为 /app/api/v1/...；直连时注意 CORS）
