# 后台管理前端 API 根地址（不要尾斜杠）。仅 VITE_ 前缀会注入 import.meta.env。
#
# 推荐：先启动后端 gateway（默认 8080），由网关处理 CORS，并将 /admin/** 转发到 admin-api。
VITE_API_BASE_URL=http://127.0.0.1:8080
#
# 直连 admin-api（默认 8099）时改为上一行地址，且须在 admin-api 设置 bff.browser-cors.enabled=true，否则浏览器跨域会失败。
